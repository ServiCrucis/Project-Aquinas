// trees: anamorphic, catamorphic

// A/B language

// bintree

// origin-origin
// origin-decider
// origin-input

'a'[N] + "ba" + 'b'[N]

{'a', 'a', 'a', 'b', 'a', 'b', 'b', 'b'} // input
{'a'[3], "ba", 'b'[3]} // decision
{abab_object} // target

<pattern-identifier> identifier := value
